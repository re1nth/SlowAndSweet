---
description: Force-delegate a task to the slm-batch subagent, bypassing the usual heuristics.
argument-hint: <task description>
---

Invoke the `slm-batch` subagent with the following task and follow its
instructions, including its abstain protocol. Do not second-guess whether
delegation is appropriate — the user explicitly asked for it.

Task: $ARGUMENTS
